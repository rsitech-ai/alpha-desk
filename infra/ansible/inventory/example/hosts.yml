---
all:
  children:
    stage0_example:
      vars:
        alpha_inventory_kind: example
        alpha_stage0_scaffold: true
      hosts:
        alpha-stage0-control.invalid:
          ansible_host: alpha-stage0-control.invalid
