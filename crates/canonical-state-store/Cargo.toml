[package]
name = "canonical-state-store"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Crash-safe local checkpoint storage for Hyperliquid Alpha Desk."
homepage.workspace = true
repository.workspace = true
readme.workspace = true
publish = false

[dependencies]
blake3.workspace = true
canonical-ledger = { path = "../canonical-ledger", version = "=0.1.0" }
domain-types = { path = "../domain-types", version = "=0.1.0" }
getrandom.workspace = true
hex.workspace = true
rustix.workspace = true
storage-ports = { path = "../storage-ports", version = "=0.1.0" }

[dev-dependencies]
canonical-events = { path = "../canonical-events", version = "=0.1.0" }
orderbook = { path = "../orderbook", version = "=0.1.0" }
tempfile.workspace = true
