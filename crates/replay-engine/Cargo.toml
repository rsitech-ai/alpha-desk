[package]
name = "replay-engine"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic replay-engine foundations for Hyperliquid Alpha Desk."
homepage.workspace = true
repository.workspace = true
readme.workspace = true
publish = false

[dependencies]
blake3.workspace = true
canonical-ledger = { path = "../canonical-ledger", version = "=0.1.0" }
domain-types = { path = "../domain-types", version = "=0.1.0" }
storage-ports = { path = "../storage-ports", version = "=0.1.0" }
thiserror.workspace = true

[dev-dependencies]
canonical-archive = { path = "../canonical-archive", version = "=0.1.0" }
canonical-events = { path = "../canonical-events", version = "=0.1.0" }
tempfile.workspace = true
